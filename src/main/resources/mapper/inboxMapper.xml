<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.sdb.buzzfeed.mapper.InboxMapper">
<!--    插入用户收件箱-->
    <insert id="insertInbox">
        INSERT INTO inbox (user_id, content_id, publish_time)
        VALUES
        <foreach collection="activeFans" item="uid" separator=",">
            (#{uid}, #{contentId}, #{publishTime})
        </foreach>
    </insert>

<!--    下拉从收件箱中获取feed流（JOIN item_info 获取内容详情和真实创作者）-->
    <select id="downFeed" resultType="org.sdb.buzzfeed.entity.Content">
        SELECT i.content_id AS item_id,
               i.publish_time,
               c.creator_id,
               c.item_type AS itemType,
               c.title,
               c.summary,
               c.visibility,
               c.status
        FROM inbox i
        LEFT JOIN item_info c ON i.content_id = c.item_id
        WHERE i.user_id = #{userId}
          AND i.publish_time &lt; #{lastTime}
        ORDER BY i.publish_time DESC, i.content_id DESC
            LIMIT #{num}
    </select>

<!--    上滑从收件箱中获取feed流（JOIN item_info 获取内容详情和真实创作者）-->
    <select id="downFeedUp" resultType="org.sdb.buzzfeed.entity.Content">
        SELECT i.content_id AS item_id,
               i.publish_time,
               c.creator_id,
               c.item_type AS itemType,
               c.title,
               c.summary,
               c.visibility,
               c.status
        FROM inbox i
        LEFT JOIN item_info c ON i.content_id = c.item_id
        WHERE i.user_id = #{userId}
          AND (i.publish_time &lt; #{lastTime} OR (i.publish_time = #{lastTime} AND i.content_id &lt; #{lastContentId}))
        ORDER BY i.publish_time DESC, i.content_id DESC
            LIMIT #{num}
    </select>

</mapper>
