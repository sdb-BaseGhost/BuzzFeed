<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.sdb.buzzfeed.mapper.InboxMapper">
<!--    插入用户收件箱-->
    <insert id="insertInbox">
        INSERT INTO inbox (user_id, content_id, publish_time)
        VALUES
        <foreach collection="activeFans" item="uid" separator=",">
            (#{uid}, #{contentId}, #{publishTime})
        </foreach>
    </insert>

<!--    下拉从收件箱中获取feed流-->
    <select id="downFeed" resultType="org.sdb.buzzfeed.entity.Content">
        SELECT content_id AS item_id,
               publish_time,
               user_id AS creator_id
        FROM inbox
        WHERE user_id = #{userId}
          AND publish_time &lt; #{lastTime}
        ORDER BY publish_time DESC
            LIMIT #{num}
    </select>

<!--    上滑从收件箱中获取feed流-->
    <select id="downFeedUp" resultType="org.sdb.buzzfeed.entity.Content">
        SELECT content_id AS item_id,
               publish_time,
               user_id AS creator_id
        FROM inbox
        WHERE user_id = #{userId}
          AND (publish_time &lt; #{lastTime} OR (publish_time = #{lastTime} AND content_id &lt; #{lastContentId}))
        ORDER BY publish_time DESC
            LIMIT #{num}
    </select>

</mapper>
